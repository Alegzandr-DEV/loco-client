import { useTranslation } from 'react-i18next';

const Privacy = () => {
  const { t } = useTranslation('privacy');

  return(
    <div className="legal">
      
    </div>
  );
};

export default Privacy;
