import { useTranslation } from 'react-i18next';

function Contact() {
  const { t } = useTranslation('contact');

  return(
    <div>
      <main role="main"></main>
    </div>
  );
}

export default Contact;
